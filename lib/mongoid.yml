#identity_map_enabled: true

development:
  sessions:
    default:
      database: maybe_later_dev
      hosts: 
        - localhost:27017

test:
  sessions:
    default:
      database: maybe_later_tst
      hosts: 
        - localhost:27017
